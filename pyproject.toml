[project]
name = "cldata"
version = "0.1.0"
description = "Fast clean and encoding data in python"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.3",
    "scikit-learn>=1.8.0",
]
